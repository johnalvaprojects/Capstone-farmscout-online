<?php

declare(strict_types=1);

use PHPUnit\Framework\TestCase;

/**
 * Minimal CI smoke test — replace with real tests as the suite grows.
 */
final class SmokeTest extends TestCase
{
    public function testCiPipelineBootstraps(): void
    {
        $this->assertTrue(true);
    }
}
